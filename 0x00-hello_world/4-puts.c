#include <stdio.h>
/**
 * main - Entry point
 * Program that prints a sentence using the function puts.
 * Return: Always 0 (Success)
 */
int main(void)
	{
		puts("\"Programming is like building a multilingual puzzle");
		return (0);
	}
