#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 * Program that prints all possible combos of single-digit numbers.
 */
int main(void)
{
	int nums;

	for (nums = 0; nums <= 9, num++)
	{
		putchar((num % num), ", ");
	}
	putchar('\n');
	return (0);
}
