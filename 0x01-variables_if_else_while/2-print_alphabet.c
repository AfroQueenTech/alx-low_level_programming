#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0
 * Program that prints the alphabet in lowercase.
 */
int main(void)
{
	char alph = 'a'

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch\n);
	return (0);
}
